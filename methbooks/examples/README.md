# Canonical examples

After the first successful `make methbook` run, this file lists the rule paths and
methodology path that the critique agent treats as canonical references. Empty until
the first run completes.
