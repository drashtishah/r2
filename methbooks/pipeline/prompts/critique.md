Start by Reading meth-pipeline/<slug>/<ts>/plan_v1.json (planner's
draft) and meth-pipeline/<slug>/<ts>/input/markdown.md. Consult the same
canonical examples under methbooks/examples/.

Fresh read; you have not seen the planner's reasoning. Compare plan
against markdown and examples. Challenge: is each new rule truly atomic,
are reuse claims correct, are thresholds placed where the
minimal-parameters rule says they should be, does composition_order
match the methodology text, are ambiguities honest.

Edit any field of the plan. Your output is the authoritative spec.
